#!/bin/bash
# Build the jirade Docker image
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

docker build -t jirade:latest -f "$SCRIPT_DIR/Dockerfile" "$SCRIPT_DIR"

echo "Built jirade:latest"
